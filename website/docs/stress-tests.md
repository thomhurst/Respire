---
title: Stress tests
description: Latest sustained Respire and StackExchange.Redis stress-test results.
---

# Stress tests

:::info Awaiting first published run
The weekly stress workflow will replace this page after its first complete run on `main`.
:::

Stress tests compare sustained throughput, latency percentiles, allocations, CPU cost, errors, stalls, and throughput drift across common Redis workloads.

Scheduled runs publish every scenario for both clients. Partial manual runs remain available as workflow artifacts but cannot replace this page.
